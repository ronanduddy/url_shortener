URL Accesses
<table>
  <tr>
    <th>#</th>
    <th>Created at</th>
  </tr>
  <% @url_accesses.each do |url_access| %>
  <tr id="<%= url_access.id %>">
    <td><%= url_access.created_at %></td>
  </tr>
  <% end %>
</table>
